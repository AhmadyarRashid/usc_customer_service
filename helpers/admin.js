const db = require("../helpers/db")
const {
    getFailureResponse,
    getSuccessResponse
  } = require("../helpers/response");

const createAdminLoginIfNotExist = async () => {
}

module.exports = {
    createAdminLoginIfNotExist
}